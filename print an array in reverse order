#include<stdio.h>
int main()
{
   int n,i,a[100];
   printf("Enter the number of elements");
   scanf("%d",&n);
   printf("\nEnter the elememts");
   for(i=0;i<n;i++)
   {
      scanf("%d",&a[i]);
   }
   for(i=n-1;i>=0;i--)
   {
      printf("%d\t",a[i]);
   }
   return 0;
}
