#include<stdio.h>
int main()
{
   int n,i,s=0,a[100];
   printf("Enter number of elements");
   scanf("%d",&n);
   printf("\nEnter the elements");
   for(i=0;i<n;i++)
   {
      scanf("%d",&a[i]);
      if(i%2==0)
      {
      s=s+a[i];
      }
   }
   printf("\nsum=%d",s);
   return 0;
   }
