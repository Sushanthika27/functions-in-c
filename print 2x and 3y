#include<stdio.h>
void star(int*x,int*y)
{
   *x=2**x;
   *y=3**y;
   printf("\n%d\t%d",*x,*y);
}
int main()
{
   int x,y;
   printf("enter the value of x,y");
   scanf("%d%d",&x,&y);
   star(&x,&y);
   return 0;
}
